import React from 'react';

const CartWidget = () => {
    return (
        <div className="cart-widget">
            {/* Ícone do carrinho (pode ser substituído por um ícone real) */}
            <i className="bi bi-cart"></i>
        </div>
    );
};

export default CartWidget;
